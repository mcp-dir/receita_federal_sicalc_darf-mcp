# Ferramentas

Receita Federal SICALC: Gerar DARF expõe 1 ferramenta (todas somente leitura).

### 1. `receita_federal_sicalc_darf_consultar`
**Input**: `cnpj` (opcional), `cpf` (opcional), `birthdate` (opcional), `observacoes` (opcional), `codigo` (opcional), `valor_principal`, `periodo_apuracao`, `data_consolidacao` (opcional), `numero_referencia` (opcional), `quota` (opcional)

Receita Federal SICALC: Gerar DARF, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Receita Federal SICALC: Gerar DARF
```
